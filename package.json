{
  "name": "@koyofinance/boba-blocks",
  "version": "1.0.0",
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "build:boba": "graph build --network boba",
    "build:aurora": "graph build --network aurora",
    "build:moonriver": "graph build --network moonriver",
    "build:matic": "graph build --network matic",
    "create:local": "graph create --node http://localhost:8020/ koyo-finance/blocks",
    "deploy:boba:mainnet": "graph deploy --product hosted-service koyo-finance/boba-blocks",
    "deploy:aurora:mainnet": "graph deploy --product hosted-service koyo-finance/aurora-blocks",
    "deploy:moonriver:mainnet": "graph deploy --product hosted-service koyo-finance/moonriver-blocks",
    "deploy:matic:mainnet": "graph deploy --product hosted-service koyo-finance/matic-blocks",
    "deploy:local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 koyo-finance/blocks",
    "remove:local": "graph remove --node http://localhost:8020/ koyo-finance/blocks"
  },
  "dependencies": {},
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.30.0",
    "@graphprotocol/graph-ts": "^0.27.0"
  },
  "resolutions": {
    "colors": "1.4.0"
  }
}
